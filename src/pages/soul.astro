---
import "../styles/global.css";
// Component Imports
import Layout from "@/layouts/Layout.astro";
import SoulComponent from "@/components/soul";
import { getCurrentGrid, listenToGridChanges } from "@/lib/supabase";

const { data, error } = await getCurrentGrid();
//const newPixel = listenToGridChanges()
---

<Layout>
  <SoulComponent initialGrid={data} client:load />
</Layout>
